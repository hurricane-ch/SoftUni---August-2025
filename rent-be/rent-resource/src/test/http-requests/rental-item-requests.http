### POST login (Set access_token to the global variables)
POST {{host}}/api/v0.0.1/auth/login
Content-Type: application/x-www-form-urlencoded

username=admin&password=rent*2025

> {%
    client.test("Request executed successfully", function() {
        if (response.status === 200) {
            client.global.set("access_token", response.body["token"])
            client.global.set("refresh_token", response.body["refreshToken"])
        }
        client.assert(response.status === 200, "Access is Denied");
    });
%}

###  RenatalItem Controller # create
POST {{host}}/api/v0.0.1/rental-items
Content-Type: application/json
Authorization: Bearer {{access_token}}

{
  "rentalItemType": "BUNGALOW",
  "name": "Number 1",
  "room": 2,
  "price": 100.00,
  "recommendedVisitors": 4
}